{
  "name": "lb-ide-host",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "LiquidBounce script that opens the LB Script IDE in-game (CEF) and loads built scripts into the client.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "package": "node scripts/package.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "license": "MIT",
  "devDependencies": {
    "@wunk/lb-script-api-types": "^0.38.4",
    "esbuild": "^0.23",
    "typescript": "^5.4"
  }
}
